---
import SpecimenCard from './SpecimenCard.astro';

interface Props {
  href: string;
  title: string;
  date: string;
  imageUrl: string | null;
  ratio: number;
  number: number;
  authorName: string;
  authorHref: string;
  authorAvatarUrl: string | null;
  regionName: string | null;
  regionHref: string | null;
  commissionedByName: string | null;
  index: number;
}
const {
  href,
  title,
  date,
  imageUrl,
  ratio,
  number,
  authorName,
  authorHref,
  authorAvatarUrl,
  regionName,
  regionHref,
  commissionedByName,
  index,
} = Astro.props;
---

{/* グリッドのカード。masonry の並べ替え・reveal 用に data-ratio/data-index/reveal を足す。 */}
<SpecimenCard
  href={href}
  title={title}
  date={date}
  imageUrl={imageUrl}
  ratio={ratio}
  number={number}
  authorName={authorName}
  authorHref={authorHref}
  authorAvatarUrl={authorAvatarUrl}
  regionName={regionName}
  regionHref={regionHref}
  commissionedByName={commissionedByName}
  class="reveal group"
  data-ratio={ratio}
  data-index={index}
/>
