---
import { cn } from '../../lib/cn';

interface Props {
  for?: string;
  id?: string;
  class?: string;
}

const { for: htmlFor, id, class: className } = Astro.props;
---
<label for={htmlFor} id={id} class={cn('text-sm font-medium leading-none', className)}>
  <slot />
</label>
